<!DOCTYPE HTML>
<html>

<head>
    <title>
        <%= username %>
    </title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />
    <link rel="stylesheet" href="/assets/css/main.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <noscript>
        <link rel="stylesheet" href="assets/css/noscript.css" />
    </noscript>
    <style>
        body {
            background: #EFF1F6;
        }

        .neumorph-card {
            border-radius: 16px;
            background: #EFF1F6;
            box-shadow: 12px 12px 24px #c1c1c1,
                -12px -12px 24px #ffffff;
        }

        .scroll-horiz {
            overflow: auto;
            white-space: nowrap;
        }

        .scroll-no-bar {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .scroll-no-bar::-webkit-scrollbar {
            display: none;
        }
    </style>

    <script type="text/javascript">
        $(document).ready(function () {

            $.ajax({
                url: "/u/<%= userId %>/project",
                type: "GET",
                async: true,
                timeout: 3000,
                beforeSend: function () {
                    $("#projects").text("");
                },
                success: function (data, status, xhr) {
                    var str = "";
                    for (var i = 0; i < data.length; i++) {
                        str += '<div class="content neumorph-card p-3">';
                        str += '<header><h6>' + data[i].title + '</h6></header>';
                        str += '<p>' + data[i].content + '</p></div>';
                    }
                    $("#projects").html(str);
                },
                error: function (xhr, status, error) { }
            });

            $.ajax({
                url: "/u/<%= userId %>/activity",
                type: "GET",
                async: true,
                timeout: 3000,
                beforeSend: function () {
                    $("#activities").text("");
                },
                success: function (data, status, xhr) {
                    var str = "";
                    for (var i = 0; i < data.length; i++) {
                        str += '<div class="content neumorph-card p-3">';
                        str += '<header><h6>' + data[i].startDate + ' ~ ' + data[i].endDate + '</h6></header>';
                        str += '<p>' + data[i].activityName + '</p></div>';
                    }
                    $("#activities").html(str);
                },
                error: function (xhr, status, error) { }
            });

            $.ajax({
                url: "/u/<%= userId %>/award",
                type: "GET",
                async: true,
                timeout: 3000,
                beforeSend: function () {
                    $("#award").text("");
                },
                success: function (data, status, xhr) {
                    var str = "";
                    for (var i = 0; i < data.length; i++) {
                        var icons = ["🏅", "🥇", "🥈", "🥉", "👍"];
                        var prizeIcon = icons[data[i].prizeIcon % icons.length];

                        str += '<div class="content neumorph-card p-3">';
                        str += '<header><h6>' + data[i].date + '</h6></header>';
                        str += '<p>' + prizeIcon + data[i].prizeName + '  [' + data[i].awardName + ']</p></div>';
                    }
                    $("#award").html(str);
                },
                error: function (xhr, status, error) { }
            });
        });
    </script>
</head>

<body class="is-preload">
    <nav class="navbar navbar-expand bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">
                <%= username %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse scroll-horiz scroll-no-bar" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="#">프로필</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">만든 작품들</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">활동기간</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">수상내역</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Wrapper -->
    <div class="d-flex justify-content-center">
        <div id="wrapper" class="divided flex-low" style="max-width: 856px;">

            <!-- Elements -->
            <section class="wrapper style1 align-center">
                <div class="inner">
                    <!-- 본인 소개 -->
                    <h2>본인소개</h2>
                    <p>
                        <% if (introduce !=undefined && introduce.length> 0) { %>
                            <%= introduce %>
                                <% } else { %>
                                    프로필을 작성해주세요!
                                    <% } %>
                    </p>
                    <!-- 본인 소개 -->
                    <h2 class="mt-5">전문 분야</h2>
                    <p>
                        <%= specialty %>
                    </p>
                    <div class="index align-left">
                        <!-- 만든 작품들 소개-->
                        <section class="mt-5">
                            <header class="mt-3 mb-3">
                                <h3>만든 작품들 소개</h3>
                            </header>
                            <div id="projects">
                            </div>
                        </section>

                        <!-- 활동 기간 -->
                        <section>
                            <header class="mt-3 mb-3">
                                <h3>활동 기간</h3>
                            </header>
                            <div id="activities">

                            </div>
                        </section>

                        <!-- 수상 내역 -->
                        <section>
                            <header class="mt-3 mb-3">
                                <h3>수상 내역</h3>
                            </header>
                            <div id="award">
                            </div>
                        </section>

                    </div>

                </div>

            </section>

        </div>

    </div>

</body>

</html>